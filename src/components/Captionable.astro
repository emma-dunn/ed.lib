---
import type { HTMLAttributes } from "astro/types";

type Props = {
  caption?: string;
};

const { caption } = Astro.props;
---

{
  caption ? (
    <figure class="my-2">
      <slot />
      <figcaption>{caption}</figcaption>
    </figure>
  ) : (
    <slot />
  )
}
