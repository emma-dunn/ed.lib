---
import Header from "./Header.astro";

interface Props {
  class?: string;
}

const { class: classes } = Astro.props;
---

<main class:list={["relative mx-auto max-w-7xl", classes]}>
  <Header class="relative z-0" />
  <div class="bg-base-100 p-5 rounded-2xl -mt-10 relative z-10">
    <slot />
  </div>
</main>
